import { test } from "node:test";
import assert from "node:assert";

test("should not throw when authorized", () => {
  // TODO: write a test that fails due to the bug in
  // SafeCalculator.add()
  assert.ok(false, "make this test fail for the right reason");
});
