import test from "tape"

test('send v1', (t) => {
  // TODO: write a test that fails due to the bug in
  // MailSender.sendV1
  t.end()
})

test('send v2', (t) => {
  // TODO: write a test that fails due to the bug in
  // MailSender.sendV2
  t.end()
})
