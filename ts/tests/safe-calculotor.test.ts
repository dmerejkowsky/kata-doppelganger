import { test, expect } from "vitest";

test("should not throw when authorized", () => {
  // TODO: write a test that fails due to the bug in
  // SafeCalculator.add()
  expect(false, "make this test fail for the right reason!").toBeTruthy();
});
